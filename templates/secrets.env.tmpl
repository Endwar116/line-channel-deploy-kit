# line_secrets.env — LINE Messaging API 金鑰（本檔=空殼模板，值由部署者親自填入）
# 安裝後位置：<base>/config/secrets/line_secrets.env，權限務必 600（install.py 會設）
# 紅線：金鑰不進版本控制、不進對話紀錄、不貼給任何 AI。
#
# 申請完整步驟見 docs/LINE_Developers_申請教學.md。摘要：
#
# ① LINE_CHANNEL_SECRET：
#    https://developers.line.biz/console/ → 你的 Provider → 你的 Messaging API channel
#    → 「Basic settings」頁 → Channel secret（32 字 hex）
LINE_CHANNEL_SECRET=

# ② LINE_CHANNEL_ACCESS_TOKEN：
#    同 channel → 「Messaging API」頁 → Channel access token (long-lived) → 按 Issue 產生
#    （外洩疑慮時：console 重 Issue 即作廢舊值，回來更新本檔）
LINE_CHANNEL_ACCESS_TOKEN=
